[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "conversor"
version = "1.0.0"
description = "Conversor de unidades - Engenharia Edition"
requires-python = ">=3.10"